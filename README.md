# Kishan Jaiswal Portfolio

## Run
npm install
npm run dev

## Build
npm run build

## Update projects
Edit `src/data/projects.js` and add another object to `projects`.


### Skill logos
Power BI and Excel use local SVG assets in `public/icons/`, so those two logos do not depend on an external icon CDN.
